node_modules
dist

# Generated third-party bundles (copied from node_modules on install)
app/client/ui/vendor/

# Generated passive-tree sprites (28MB, regenerate with scripts/build-tree-{sprites,backgrounds}.cjs)
app/media/tree/0_5/sprites/

# Vite dev cache
.vite/

# Webdriver / automation browser profiles (not source)
tools/webdriver/

# Old app kept for reference only — new Lua/Bootstrap app lives at the repo root
concept1/

# Local-only tooling / scratch
.claude
mcps
tmp-*.filter

# Electron desktop build output
release
dist-electron

# Playwright MCP scratch output
.playwright-mcp

# Local env (Supabase publishable key is public, but keep env files out of git)
.env
.env.local
.env.*.local

# Legacy vendored filter sources — superseded by the from-scratch generator, kept local only.
/corefilters/
/NeverSink-Filter-for-PoE2-0.10.1a/

# Private Supabase edge function — kept local, not in the repo
supabase/functions/market/

# Internal design notes
/docs/superpowers/
.cache/

# pob-web: the 500MB build workspace stays out of git; the assembled bundle
# (app/vendor/pobweb + the app/vendor/dist runtime) IS committed so the released
# .exe ships a working, offline Build Planner. (`dist` on line 2 would otherwise
# eat app/vendor/dist — re-include it explicitly.)
/pobweb/
!/app/vendor/dist/
!/app/vendor/dist/**
